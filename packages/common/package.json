{
  "name": "xlorem-common",
  "author": "gusalbukrk",
  "version": "0.0.3-alpha.0",
  "description": "Common files for xlorem monorepo.",
  "license": "MIT",
  "keywords": [],
  "type": "module",
  "engines": {
    "node": ">=14.0.0"
  },
  "files": [
    "src",
    "!src/**/__tests__/",
    "!src/**/__spec__/",
    "!src/**/*.test.[jt]s",
    "!src/**/*.test.[jt]sx",
    "!src/**/*.spec.[jt]s",
    "!src/**/*.spec.[jt]sx"
  ],
  "scripts": {
    "build": "echo 'no `build` script at `xlorem-common`' && exit 0",
    "lint": "eslint src/ --fix && cspell \"**\" -c ../../cspell.config.cjs --no-progress",
    "test": "jest --passWithNoTests"
  }
}
